- [JS not TS](project_js_not_ts.md) — components are actually .js+JSDoc, not .ts as CLAUDE.md's skeleton implies; HTMLElementTagNameMap lives only in components/elements.d.ts, never inline `declare global` in a .js file
- [Group/child coordination patterns](pattern_group_child_coordination.md) — slot-query getters + bubbling activate events + host keydown roving tabindex (tabs/list); RadioSelectionController for form-adjacent groups without a wrapper; validated building md-segmented-button-group (roving-tabindex init gotcha) and retrofitting md-radio (WeakMap-refcounted group keydown listener, root-node group fallback, disabled-cascade MutationObserver bug)
- [dist/ build naming quirk](build_dist_naming_quirk.md) — CSS?inline chunk collides with same-named component JS, produces a "foo2.js"; plain foo.d.ts sometimes missing (pre-existing, e.g. md-button too) — check before wiring a package.json subpath export
- [Composition wrapper pattern](pattern_composition_wrapper.md) — building a component that composes other md-* components (md-split-button + md-button/md-icon-button): md-button's `variant` isn't Lit-reactive (use `.prop=` bindings, not attribute bindings), `::part()` overrides win over a child's internal rules regardless of specificity, and disabled+click tests must click the inner native element not the wrapper
- [PopoverPositionController](pattern_popover_position_controller.md) — shared/popover-position-controller.js backs md-menu, built for reuse by a future md-tooltip; confirms via a real Playwright probe that native popover ancestor-stacking (`showPopover({source})`) does NOT keep a parent popover open across separate/sibling shadow roots — manual cascade-close fallback required, plus a known unfixed gap (click inside parent surface while a submenu is open closes the whole chain)
- [md-select menu-mode combobox/ARIA](pattern_menu_select_combobox_aria.md) — Google Material Web reference behaviors for select/menu/menu-item ARIA+keyboard; the opt-in `md-menu-item.type`/`md-menu.menuRole`/`md-menu.focusOnOpen` properties this codebase added to match; why select manages combobox ARIA on its own trigger button rather than via md-menu's auto trigger wiring; a Lit `updated()`-fires-unawaited-async-effect gotcha found in `md-menu.show()`
